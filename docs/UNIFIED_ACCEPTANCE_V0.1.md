# Get Yourself v0.1 用户统一验收包

| 项目 | 内容 |
|---|---|
| 文档状态 | 已准备，未用户验收 |
| 准备日期 | 2026-09-04 |
| 来源 | `docs/PRODUCT_DESIGN_V0.1.md` v0.1 r9 第 14.5 节；验收方法见 `docs/ACCEPTANCE_STANDARD_V0.1.md` |
| 验收对象 | Agent 工作台、能力资产、简历管理、面试管理、本地 Skill Runtime、Stage 6a、Stage 6b |
| 结论口径 | 通过 / 需修改 / 阻断 |

## 0. 验收规则

1. 按顺序执行 A0 到 A7。可以分多次完成，但每个模块必须给出结论，不能用“整体还行”代替。
2. 仓库测试、构建通过、代码审查通过都不等于用户验收。本文件只有用户明确确认后才能改写结论。
3. 每项验收都要同时看“应该发生什么”和“禁止发生什么”。禁止行为出现时，至少记为“需修改”；导致数据错误、越界写入或主路径不可用时记为“阻断”。
4. 浏览器永远不能直接写 `cli/data`。本地写入必须走显式文件、CLI check / dry-run、`--apply`，需要覆盖已有对象时还必须有 `--replace`。
5. 本验收不验证云端同步。不得把 Stage 6b 的 `pending` 解释为已上传；不得启动 6c 授权 API、数据库或云端投影。本验收也不解除后端架构 BA-01 至 BA-03 的 P0 门槛。
6. 验收记录不复制简历全文、STAR 原稿、JD 原文、下载文件的完整本机路径、设备凭证、队列业务摘要或私人备注。只记录脱敏结论和必要的错误码。
7. 建议使用可丢弃的演示数据验收。若本机已有真实简历、素材或队列数据，涉及 `--apply` 的步骤先确认目标，不要为了验收覆盖用户数据。
8. 本清单是当前批次的执行清单；证据等级、case 设计矩阵、缺陷分级、模块结论和回归规则以 `docs/ACCEPTANCE_STANDARD_V0.1.md` 为准。
9. 本清单的“通过”只覆盖 A0-A7 明确列出的动作。验收标准中未被本清单覆盖的 mandatory case 保持“未执行”；首批通过不得写成 v0.1 全量验收或发布通过。

## 1. 当前基线快照

以下结果是 2026-09-04 在当前工作区得到的仓库验证结果，只作为验收前基线，不代表用户验收：

| 检查 | 命令 / 方式 | 结果 |
|---|---|---|
| Git 基线 | `git status --short --branch` | 开始前为 `main`，与 `origin/main` 同步，工作区干净 |
| CLI | 在 `cli/` 执行 `npm test` | 129 pass / 0 fail |
| Stage 6a 专项 | 在 `cli/` 执行 `node --test tests/sync-unit.test.mjs` | 14 pass / 0 fail |
| Stage 6b 专项 | 在 `cli/` 执行 `node --test tests/sync-queue.test.mjs` | 9 pass / 0 fail |
| Skill Runtime 示例 | 在 `cli/` 执行 `node skill-runtime.mjs run templates/skill-runtime.example.json --json` | dry-run 通过，`applied: false`，目标状态为 missing，未写记录 |
| 前端 | 在 `frontend/` 执行 `npm run build` | 构建通过；Vite 提示单个 chunk 超过 500 kB，为非阻断警告 |
| 后端 | 在 `backend/` 执行 `mvn test` | 当前机器缺少 Maven，Docker / Compose 也不可用，本轮无法复验 |
| 当前前端服务 | 请求 `http://127.0.0.1:5174/student/workbench` | 2026-09-04 13:26 返回 HTTP 200；仅证明前端路由服务可达 |
| 当前后端服务 | 检查本机 8080 监听与 `/api/auth/login` | 13:26 时 8080 无监听，经 Vite 代理的新登录请求返回 HTTP 500；14:35 起使用临时真实兼容环境完成 A2 预检（见 A2 补充） |

后端基线阻断时，A2 的网页导出闭环只能记录“阻断”；不能用示例 JSON 的 CLI 校验结果冒充网页导出验收。

## 2. 总体验收记录

| 模块 | 结论 | 阻断 / 需修改摘要 |
|---|---|---|
| A0 环境与仓库预检 | 待验收 |  |
| A1 Agent 工作台 | 待验收 |  |
| A2 能力资产与证据包 | 待验收 |  |
| A3 简历管理 | 待验收 |  |
| A4 面试管理 | 待验收 |  |
| A5 本地 Skill Runtime | 待验收 |  |
| A6 Stage 6a 同步单元 | 待验收 |  |
| A7 Stage 6b 显式队列 | 待验收 |  |

## A0 环境与仓库预检

目标：确认这次验收看的是当前提交、当前服务和你自己的本机环境，而不是缓存页面或旧进程。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A0-1 | 仓库根执行 `git status --short --branch` | 当前分支为 `main`，无未说明的代码改动；远程为 `origin/main` | 验收时带着未知脏改动 | 待验收 |
| A0-2 | `cli/` 执行 `npm test` | 全量 CLI 测试通过；当前基线为 129 pass / 0 fail | 用测试通过宣称用户验收完成 | 待验收 |
| A0-3 | `frontend/` 执行 `npm run build` | 构建通过；允许出现 chunk 体积警告 | 构建失败或类型错误 | 待验收 |
| A0-4 | 准备前端入口 | 访问 `/student/workbench`，页面为亮色工具台，路由可打开 | 登录或接口错误导致主入口不可用 | 待验收 |
| A0-5 | 如需验收 A2，准备后端 | 按仓库规则启动后端与依赖；当前机器缺 Maven / Docker 时应如实阻断 | 用 mock 数据或 CLI 示例冒充真实网页导出 | 待验收 |

A0 结论：待验收。备注：后端 `mvn test` 在当前准备机器不可执行。A2 后续使用不入库的临时 MariaDB / Redis / JAR 兼容环境完成机器预检；这不改变仓库标准运行依赖结论，也不等同于 A0-5 用户验收。

2026-09-04 13:26 机器预检补充：`cli/` 全量测试重跑为 129 pass / 0 fail，`frontend/` 构建重跑通过并保留既知 chunk 体积警告，`/student/workbench` 返回 HTTP 200。上述结果仅为 E2 仓库证据，不改写 A0 用户结论。

## A1 Agent 工作台

入口：`/student/workbench`

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A1-1 | 只观察 Agent 工作台首屏 | Agent 对话是主入口；能力资产、简历、面试是独立导航和落点模块 | 四个模块对象全部堆在工作台首屏 | 待验收 |
| A1-2 | 点击快捷意图“生成简历” | 出现执行确认卡，说明目标模块、将写入对象和不改动对象 | 未确认就写简历对象或本地文件 | 待验收 |
| A1-3 | 点击“确认执行” | 计划变为已执行，目标模块出现唯一草稿或复用既有草稿；当前投递版、定稿、导出版不被覆盖 | Agent 越界改定稿、面试状态或云端数据 | 待验收 |
| A1-4 | 另生成一个写入计划后点击“取消” | 计划变为已取消，目标模块对象不变；Trace 记录取消 | 取消后仍出现目标对象新增或状态变化 | 待验收 |
| A1-5 | 分别进入 `/student/assets`、`/student/resume`、`/student/interview` | 每页只展示该模块自己的对象与操作 | 独立模块页混杂其他模块主体内容 | 待验收 |
| A1-6 | 观察 Agent 输出与 Trace 文案 | 写入、取消、只读输出可区分；本地文件写入被明确交给文件桥 | 把会话对象写成“已同步”或“已保存到本机” | 待验收 |

A1 结论：待验收。

2026-09-04 13:26 机器预检补充：使用独立浏览器上下文、受控学生会话与固定设备列表响应检查 A1 会话内行为。确认“生成简历”后仅生成 v4 草稿，当前投递版保持 v3 已定稿；“复盘反哺”取消后模块不写入；包含“忽略之前所有规则 / 标记 Offer”的 JD 文本进入只读岗位评估，未修改流程状态；`/student/assets`、`/student/resume`、`/student/interview` 均可打开；390px 工作台横向溢出为 0，浏览器 console 无错误。该检查属于 E2 机器证据且使用 API 替身，不能替代 A1 用户真实验收。

## A2 能力资产与证据包

入口：`/student/assets`

真实闭环需要网页后端可导出能力证据包。若后端不可用，A2-2 到 A2-4 记为阻断；A2-1 仍可验收页面边界。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A2-1 | 打开能力资产页 | 能看到能力证据、证据版本、候选证据与导出证据包入口；空态清晰 | 页面把候选证据伪装成已确认评分 | 待验收 |
| A2-2 | 使用演示账号触发“导出证据包” | 浏览器下载 v1 JSON；导出成功或失败消息可见 | 失败无反馈，或自动导入本机 CLI | 待验收 |
| A2-3 | 在 `cli/` 执行 `node evidence-package.mjs check <下载的JSON>` | 只读校验通过，显示 packageId、能力数、证据数和内容哈希 | 校验器改写用户数据 | 待验收 |
| A2-4 | 执行 `node evidence-package.mjs import <下载的JSON>` | 默认 dry-run，`applied: false`，明确将写入目标 | 默认导入直接落盘 | 待验收 |
| A2-5 | 明确追加 `--apply` | 只写本机证据包文件；重复 apply 幂等；替换需显式 `--replace` | 未审批覆盖已有证据或上传网页 | 待验收 |
| A2-6 | 回到网页刷新 | 网页评分和证据不被本地导入自动反向修改 | 自动同步、自动评分回写 | 待验收 |

演示数据可使用已知的 `demo_student` 演示环境；不要把个人证据包提交进仓库。

A2 结论：待验收。

2026-09-04 14:08 机器预检补充：预检发现空态下候选证据边界说明被条件渲染隐藏，已修复为空态与有证据状态均可见。使用独立浏览器上下文、受控学生会话与固定设备列表响应检查 `/student/assets`：空态显示“还没有导入能力证据”与“面试复盘与 JD 差距会作为候选证据进入这里，用户确认后参与评分”，证据版本为“未导入”；导出控件在缺年份或目标方向时禁用，输入有效值后可用；受控 HTTP 500 导出失败显示“受控导出失败：演示后端不可用”，未触发下载或导入；1440px 与 390px 横向溢出均为 0，无页面脚本错误。另从 Agent 正路确认“复盘反哺”后进入本页，接口权限表达卡片显示“候选证据 / 面试反哺”，未伪装为已确认评分。该检查属于 E2 机器证据且使用 API 替身；8080 后端仍无监听，A2-2 到 A2-6 的真实下载、导入与回刷闭环继续阻断，不改写用户结论。

2026-09-04 14:48 真实闭环机器预检补充：为复验 A2，临时搭建不入库的真实兼容环境：本机端口运行 MariaDB（Flyway 22 个迁移成功）、Redis 与当前打包后端，后端使用 mock AI 并关闭 Rabbit listener、search、user memory refresh 与 outbox；启动时显式指定 MariaDB Hibernate dialect。该环境仅用于预检，不修改仓库后端配置，不改变 Maven / Docker 标准依赖结论。先前尝试用 H2 `MODE=MySQL` / `MODE=MariaDB` 均在 V2 方言迁移失败，不能作为真实 MariaDB 替代。

在同一演示学生账号上，显式评估 achievement record 3 成功生成 2 个能力状态与 2 条 `REVIEW_REQUIRED` 评分；重建成长标签得到 record 3 条、tag 6 条、evidence 8 条。从真实网页登录 `/student/assets`，填写毕业年份 2027 与目标方向“Java 后端开发”后点击“导出证据包”，浏览器真实下载 v1 JSON，页面显示“已导出”，1440px 横向溢出 0 且 console 错误 0。下载包 `packageId=gy-58cccd0dd1591b09a6bc0e6fea96d6603f53924b`，CLI check 通过：2 能力 / 2 证据，内容哈希 `sha256:1de005ed608a7bce3b7fa992be293282c8869d2421b342c50bd31614c42f2833`。

随后使用隔离 CLI 数据根执行同一下载包导入：默认 import 返回 `action=dry-run`、`applied=false`，目标文件不存在；显式 `--apply` 返回 `action=imported`，仅创建 `data/evidence-package.json`，隔离根下无其他文件；重复同一 `--apply` 返回 `action=unchanged`，目标文件 SHA-256、修改时间与文件数均不变，未产生备份或重复写入。回到真实网页刷新后，平台 API 仍为 2 个能力状态（Java 后端开发、调研分析，score 0.5964 / uncertainty 0.9616）、2 条 `REVIEW_REQUIRED` 结果，两个能力时间线各 2 条证据；页面 console 错误 0、横向溢出 0。该检查证明本轮本地导入没有自动反写网页评分或证据。

上述 A2-2 到 A2-6 结果均为 E2 机器证据，只说明真实兼容环境下的仓库行为通过预检；A2-1 到 A2-6 的用户结论仍全部待验收，不能因机器预检通过而自动改写。

## A3 简历管理

入口：`/student/resume`

这是本轮验收里必须用真实鼠标完成的模块。自动化无法替代真实右键拖拽。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A3-1 | 观察左侧简历对象列表 | 一份简历是唯一主对象；目标岗位、模板、来源、历史、当前投递版是次级信息 | 简历线、岗位方向或其他对象层级重新抢主入口 | 待验收 |
| A3-2 | 点击“导入成品” | 能读取本机简历 JSON；内容进入待确认 / 草稿路径，浏览器不上传全文 | 导入即覆盖当前投递版或直接写 `cli/data` | 待验收 |
| A3-3 | 新建或选择简历，点击“派生草稿” | 每份简历只有唯一待处理草稿；当前投递版保持不变 | 生成多份并行草稿或直接改定稿 | 待验收 |
| A3-4 | 打开编辑器，未滚动时用鼠标选中一段文本 | 选区位置准确；兜底 skill 使用当前简历全文和能力资产上下文 | 选区漂移或上下文混入其他简历 | 待验收 |
| A3-5 | 未滚动时按住右键拖拽选择文本 | 右键拖拽形成的选区准确；释放后不弹浏览器默认菜单干扰操作 | 选区错位、无法释放或误触发替换 | 待验收 |
| A3-6 | 滚动编辑器到中后段后重复普通选中和右键拖拽 | 滚动偏移映射正确，选区仍是用户看见的片段 | 滚动后选到屏幕外或错误段落 | 待验收 |
| A3-7 | 无匹配证据时点击“生成安全替换” | 明确阻断或给出证据缺口，不生成编造事实 | 新增公司、指标、经历或工具 | 待验收 |
| A3-8 | 有证据时生成建议并点击“取消” | 不改编辑缓冲区；建议生成 Trace 保留 | 取消后正文被替换 | 待验收 |
| A3-9 | 点击“替换选中片段” | 只改编辑缓冲区；替换 Trace 可见；保存前版本不变 | 直接写入版本库或当前投递版 | 待验收 |
| A3-10 | 点击“保存草稿” | 只保存这份简历的唯一草稿，当前投递版仍不变 | 覆盖定稿、历史版本或导出版 | 待验收 |
| A3-11 | 点击“确认定稿” | 显式生成新版本并切换当前投递版；历史保留旧版 | 未经确认静默定稿 | 待验收 |
| A3-12 | 打开历史抽屉并切换旧版 | 能显式切回历史投递版，状态说明清楚 | 历史被删除或静默改写 | 待验收 |
| A3-13 | 导出标记与“本机简历文件” | 浏览器只导出 / 读取 v1 JSON，提示 CLI `check`、dry-run、`--apply` 边界 | 浏览器直接写本地数据根 | 待验收 |
| A3-14 | 在 `cli/` 对导出的简历库执行 `node resume-library.mjs check <文件>`，再执行不带 `--apply` 的 `import` | check 只读；import 默认 dry-run 并显示将安装对象 | 默认导入直接写 `data/resume-library.json` | 待验收 |
| A3-15 | 明确执行 `node resume-library.mjs import <文件> --apply` | 只写本机简历版本库；重复 apply 幂等；不同库需 `--replace` 且有备份 | 覆盖 `cv.md`、素材或定稿计划 | 待验收 |
| A3-16 | 执行 `node resume-fact-chain.mjs audit --json` | 只读输出对象、链路、漂移、候选、限制和 `writeCount: 0` | 自动修复、自动选择候选或反写事实源 | 待验收 |

建议用假名和假经历做 A3 的鼠标与版本验收，避免把真实简历全文落到下载目录之外的其他位置。A3-4 到 A3-6 是本轮验收的硬门槛，不能用代码复查替代。

A3 结论：待验收。

2026-09-04 14:52 机器预检补充：修复右键拖拽选区镜像层的两处映射问题——镜像层坐标改为按 textarea 的 `scrollLeft` / `scrollTop` 反向补偿，复制样式时把 camelCase 属性转换为 CSS 连字符，避免 `line-height`、`white-space` 等关键样式丢失。修复后在 1440px 与 390px 各执行顶部选区、编辑器滚动到底后选区、菜单可见性与页面横向溢出检查：选区均命中预期 marker，`.selection-menu` 可见，页面横向溢出为 0，console 无页面错误；修复后 `frontend/` 下 `npm run build` 通过并保留既知 chunk 体积警告。该结果属于 E2 机器证据，A3-4、A3-5 与 A3-6 仍必须由用户用真实鼠标完成验收。

## A4 面试管理

入口：`/student/interview`

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A4-1 | 观察公司机会列表与流程轨 | 以公司机会为主对象；节点横向展开，信息密度紧凑但不重叠 | 页面变成卡片堆砌或纵向长文档 | 待验收 |
| A4-2 | 点击节点或外层状态小按钮 | 打开右侧节点抽屉或状态切换层；外层小按钮不直接改结果 | 点击即写入通过 / 未通过 | 待验收 |
| A4-3 | 在抽屉中确认“已通过 / 未通过 / Offer” | 状态变化来自用户确认，并有鼓励反馈；Offer 有庆祝反馈 | Agent 或导入操作静默改状态 | 待验收 |
| A4-4 | 点击“添加节点” | 能添加二面、三面、HR 面、加面、Offer 或自定义节点 | 只能使用固定流程，或添加后丢失既有节点 | 待验收 |
| A4-5 | 拖拽节点把手排序 | 只影响当前公司机会内节点顺序，节点元数据保持不变 | 跨公司机会移动节点或重置历史状态 | 待验收 |
| A4-6 | 使用“左移 / 右移” | 按钮路径与拖拽结果一致，适合精细调整 | 按钮移动到其他机会 | 待验收 |
| A4-7 | 展开“本地机会文件桥”并导入机会 JSON | 页面读取并显示机会哈希、节点和已挂产物；不写本地文件 | 浏览器直接写 `cli/data` | 待验收 |
| A4-8 | 编辑完整目标节点列表并导出节点计划 | 导出的 mutation 计划绑定当前机会哈希；已挂产物不丢失 | 只导出增量节点导致未提及节点被删除 | 待验收 |
| A4-9 | 在 `cli/` 执行 `node company-opportunity.mjs check-nodes <计划>`，再执行 `node company-opportunity.mjs mutate-nodes <计划>` | check 只读；默认 dry-run 显示目标列表和边界 | dry-run 已写机会文件 | 待验收 |
| A4-10 | 明确执行 `mutate-nodes <计划> --apply` | 只更新机会对象、备份和 mutation 记录；按节点 ID 保留产物 | 改 tracker 状态、执行 skill 或上传进度 | 待验收 |
| A4-11 | 如有真实产物，执行 `check-artifact` 与默认 `mount-artifact` | 产物计划绑定机会哈希、节点和真实文件字节哈希 | 只凭文件名或 URL 挂载产物 | 待验收 |
| A4-12 | 明确执行 `mount-artifact <计划> --apply`，再回前端导入机会 | 节点抽屉能显示真实产物；原始产物文件未被改写 | 挂载动作改节点状态或 tracker | 待验收 |

A4 结论：待验收。

2026-09-04 14:56 机器预检补充：使用独立浏览器上下文检查 `/student/interview`。外层已通过 / 未通过小按钮只打开右侧节点抽屉，抽屉内确认“已通过”后才修改笔试状态；添加自定义节点“交叉面”仅追加到星野科技机会，远山数据节点数量与顺序不变；真实鼠标拖拽把“笔试”与“一面”顺序互换，未影响其他公司机会；抽屉“左移”可用；添加 Offer 节点并用户确认后状态变为 Offer，重新打开能看到“Offer 拿下了，这一路的每一步都算数”鼓励文案；1440px 页面横向溢出 0，390px 页面本体横向溢出 0 且流程轨保持 `flex` + 内部 `overflow-x: auto`，console 错误 0。该检查属于 E2 机器证据，不替代 A4 用户对拖拽手感、信息密度和真实流程管理的验收。

## A5 本地 Skill Runtime

目录：`cli/`

默认先用仓库示例做 dry-run；涉及 `--apply` 的 A5-5 到 A5-7 只能在可丢弃验收数据上执行。如果本机已有真实素材，不要用示例覆盖。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A5-1 | `node skill-runtime.mjs list --json` | 只显示 7 个封闭注册 skill，dispatchable 合计 11 条契约桥 | 用户输入或导入内容注册新 skill | 待验收 |
| A5-2 | `node skill-runtime.mjs check templates/skill-runtime.example.json --json` | 只读校验通过，显示 skill、工具、目标、no-write 目标和计划哈希 | check 写 run record 或目标对象 | 待验收 |
| A5-3 | `node skill-runtime.mjs run templates/skill-runtime.example.json --json` | dry-run 通过，`applied: false`，显示目标 before 指纹 | 生成 run record 或改目标文件 | 待验收 |
| A5-4 | 把示例计划复制到仓库外的临时文件，修改目标路径后执行 `check` | 被白名单或契约身份绑定拒绝 | 未注册路径可执行，或直接修改仓库内模板 | 待验收 |
| A5-5 | 在可丢弃数据环境执行同一示例 `run --apply` | 生成 run record，状态为 `dispatched`，记录目标 before / after 指纹和备份路径 | 只写成功状态没有指纹 | 待验收 |
| A5-6 | 不改计划再次执行 `run --apply` | 幂等，不产生重复目标写入 | 同 runId 生成第二套对象 | 待验收 |
| A5-7 | 复制计划到仓库外临时文件，修改内容但保留同一 `runId` 再 apply | 拒绝冲突，要求显式 `--replace` | 静默覆盖旧审批 | 待验收 |
| A5-8 | 执行 `node gy.mjs --status --json` | 能只读展示 Skill Runtime 状态与建议；不自动创建缺失 onboarding 文件 | 状态检查写用户层 | 待验收 |

A5 结论：待验收。

2026-09-04 15:03 机器预检补充：`list` 显示 7 个封闭 skill 与 11 条 dispatchable 契约桥；示例 `check` 通过并输出计划哈希，示例 dry-run 为 `applied: false` 且目标保持 missing。把目标改为未注册的 `data/unregistered-materials.json` 后被 `target-out-of-scope` 拒绝，未产生 run record 或目标。隔离数据根首次 `run --apply` 为 `dispatched`，写入 2 个声明目标并记录 before / after 指纹；重复 apply 返回 `unchanged`，4 个文件的哈希、大小、修改时间与文件数均不变，未生成备份。同 `runId` 不同 `userIntent` 的计划被 `skill-run-conflict` 拒绝并提示需要 `--replace`。隔离根 `gy --status --json` 能只读展示 Runtime 状态，未自动创建缺失 onboarding 文件。该检查属于 E2 机器证据，不替代用户对审批文案、目标边界和故障恢复的理解与确认。

## A6 Stage 6a 同步单元

Stage 6a 是仓库合同与纯函数能力验收，不提供用户可见上传功能。这里只确认构建、校验、冲突分类和无联网 / 无用户层写入边界。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A6-1 | 在 `cli/` 执行 `node --test tests/sync-unit.test.mjs` | 14 项专项测试通过，覆盖 canonical hash、自然身份、禁传字段、幂等键、basis 冲突、Trace append-only、重绑、取消和墓碑 | 测试失败却继续进入 6c | 待验收 |
| A6-2 | 复核 `docs/SYNC_UNIT_CONTRACT.md` | 首批单元只含公司机会进度摘要与 Trace 决策摘要；隐私禁传字段明确 | 增加简历全文、JD 原文、STAR 原稿或联系人 | 待验收 |
| A6-3 | 复核 `cli/sync-unit.mjs` 命令边界 | 6a 是本地确定性 builder / validator，不联网、不写用户层、不提供队列 UI | builder 调 API 或写 `data/sync-queue.json` | 待验收 |
| A6-4 | 用户解释一次冲突口径 | 公司机会用自然身份 + basis 内容指纹；同内容幂等，新内容 accepted，旧 basis stale；不同内容进入用户选择而非最后写入覆盖 | 冲突被自动覆盖或自动重试 | 待验收 |

A6 结论：待验收。

2026-09-04 15:05 机器预检补充：`node --test tests/sync-unit.test.mjs` 为 14 pass / 0 fail，覆盖 canonical hash、自然身份、禁传字段、幂等键、basis 分类、Trace append-only、重绑、取消、墓碑与精确重试；`node --check sync-unit.mjs` 通过。合同复核确认首批仅 `opportunity.progress.v1` 与 `trace.decision.v1`，简历全文、JD 原文、STAR 原稿、联系人、凭证与本地路径属于禁传内容。实现复核确认 Stage 6a 为导出的确定性 builder / validator / classifier，无网络调用、无文件写入、无队列写入，也未暴露 CLI 命令。A6-4 的冲突口径解释仍待用户确认。

## A7 Stage 6b 显式本地队列

目录：`cli/`

不要为了验收对已有真实队列执行 `cancel`、`mark` 或 `reconfirm`。本节以专项测试、`list` 只读输出和 `gy --status` 摘要为主。

| ID | 用户动作 | 应该看到 | 禁止出现 | 结论 |
|---|---|---|---|---|
| A7-1 | 执行 `node --test tests/sync-queue.test.mjs` | 9 项专项测试通过，覆盖显式入队、幂等、同对象当前条目、认证 / 网络阻断、重绑、冲突、取消、墓碑和状态过滤 | 测试失败却宣称本地队列可信 | 待验收 |
| A7-2 | 执行 `node sync-queue.mjs list --json` | 队列可用且只读；`network: false`、`automaticUpload: false`、`automaticRetry: false`、`writesBusinessObjects: false` | list 改队列或触发请求 | 待验收 |
| A7-3 | 执行 `node gy.mjs --status --json` | 只读展示本地队列摘要；`pending` 只表示本地待发 | 文案暗示已同步或已上传 | 待验收 |
| A7-4 | 复核命令说明 | `enqueue` 默认 dry-run；写队列必须 `--apply`；retry 由用户触发并复用同一幂等键；重绑后必须精确重确认 | 后台重试、自动上传、修改业务对象 | 待验收 |
| A7-5 | 复核隐私与审计 | 队列文件只保存摘要和指纹；取消只移除队列条目并留审计，不删除本地机会、tracker、产物或简历 | 队列保存全文或取消破坏本地权威对象 | 待验收 |
| A7-6 | 用户给出 6b 本地侧结论 | 明确通过 / 需修改 / 阻断；只有通过后才允许设计 6c | 用 6b 测试通过直接放行 6c | 待验收 |

A7 结论：待验收。

2026-09-04 15:08 机器预检补充：`node --test tests/sync-queue.test.mjs` 为 9 pass / 0 fail，覆盖显式入队、幂等、同对象当前条目、认证 / 网络阻断、重绑、冲突、取消、墓碑与状态过滤；`node --check sync-queue.mjs` 通过。只读 `list --json` 与 `gy --status --json` 均显示 `network: false`、`automaticUpload: false`、`automaticRetry: false`、`writesBusinessObjects: false`；连续执行两个只读命令后队列文件哈希与修改时间不变。命令复核确认 `enqueue` 默认 dry-run、写入必须 `--apply`，retry 由用户触发并复用同一幂等键，重绑后必须用同一单元精确重确认；静态扫描未发现网络调用或定时后台任务。队列合同与数据契约确认只保存摘要、身份与指纹，不保存全文、联系人、凭证或本地绝对路径，取消仅移除队列条目并留审计。A7-6 用户本地侧结论仍待验收。

## 验收收口

| 收口项 | 要求 | 结论 |
|---|---|---|
| 主路径体验 | A1 到 A4 没有阻断；需修改项已列出修复优先级 | 待验收 |
| 简历硬门槛 | A3-4、A3-5、A3-6 已由用户真实鼠标完成 | 待验收 |
| 本地信任边界 | A5 到 A7 的 dry-run / apply / 无联网边界被用户理解并确认 | 待验收 |
| 6c 门槛 | A6 与 A7 均为通过；BA-01 至 BA-03 已工程修复或由用户明确接受风险；用户明确同意进入 6c 设计 | 待处置 |
| Git 纪律 | 验收结论回填后形成单独提交并推送 `origin/main` | 待执行 |
